name: Every 2 Minutes Workflow

on:
  schedule:
    - cron: "*/2 * * * *"   # runs every 2 minutes automatically

jobs:
  frequent_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print timestamp
        run: echo "Workflow triggered at $(date -u) UTC"
